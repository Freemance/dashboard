import { makeStyles, createStyles } from '@material-ui/styles';
import { ITheme } from 'src/utils/types';

const useProfileTimeLineStyles = makeStyles((theme: ITheme) =>
  createStyles({
    root: {},
  })
);

export default useProfileTimeLineStyles;
